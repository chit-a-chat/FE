import { lightPalette } from "@shared/ui/theme/palette";

import styled from "@emotion/styled";

export const Divider = styled.div`
    width: 1px;
    height: 42px;
    background-color: ${lightPalette.grey[2]};
`;
